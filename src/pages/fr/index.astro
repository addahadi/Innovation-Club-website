---
import SeoHeader from '../../Layouts/SeoHeader.astro';
import Hero from '../../Components/Landing/Hero';
import fr from '../../../language/fr/Home.json' assert { type: 'json' };
import fr_navbar from '../../../language/fr/Navbar.json' assert { type: 'json' };
import Navbar from '../../Layouts/Navbar.astro';
import About from '../../Components/Landing/About';
import Chart from '../../Components/About/Chart.tsx';
import FooterSection from '../../Layouts/Footer/FooterSection';
import "../../styles/global.css";
import Squares from '../../Components/Ui/Squares.jsx';
import CarouselEvent from '../../Components/Events/CarouselEvent.jsx';
import FAQ from '../../Components/Faq.tsx';
---

<SeoHeader title="Innovation with Astro">
    <Navbar language={fr_navbar} />
    <div class="absolute inset-0 min-h-screen w-full">
        <Squares
            speed={0.5}
            squareSize={80}
            direction="diagonal"
            borderColor="#ffffff4d"
            hoverFillColor="#15803D"
            client:load
        />
    </div>

    {/* Hero takes full height */}
    <div class="min-h-screen">
        <Hero language={fr} client:load/>
    </div>

    {/* Content sections with proper spacing */}
    <div class="relative z-10">
        <section class="py-12 md:py-20">
            <About language={fr} client:load/>
        </section>

        <section class="py-12 md:py-20">
            <CarouselEvent language={fr} client:load/>
        </section>

        <section class="py-12 md:py-20">
            <FAQ language={fr} client:load/>
        </section>

        <FooterSection language={fr.Footer} client:visible />
    </div>
</SeoHeader>